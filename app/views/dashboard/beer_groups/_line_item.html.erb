<% if beer_group.visible %>
	<li class="large">
<% else %>
	<li class="large hidden">
<% end %>

		<a href="#"><strong><%= link_to beer_group.name, [:dashboard, beer_group] -%></strong></a>
		<div class="btn-bar">
			<span class="small">View Order: <%= beer_group.order -%></span>
			<span class="divider"></span>
			<%= link_to raw('<i class="fa fa-pencil" aria-hidden="true"></i> Edit'), edit_dashboard_beer_group_path(beer_group), :class => 'btn' %>
			<%= link_to raw('<i class="fa fa-trash" aria-hidden="true"></i> Delete'), dashboard_beer_group_path(beer_group), method: :delete, data: { confirm: 'Are you sure?' }, :class => 'btn' %>
		</div>
	</li>
